import React, { useState } from "react";
import { Slide } from "@material-ui/core";
import Card from "./hovers";
import data from "../data";

const Team = (props) => {
  return (
    <>
      <Card />
    </>
  );
};

export default Team;
